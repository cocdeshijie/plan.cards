# SECRET_KEY is auto-generated on first run if left as default
SECRET_KEY=change-this-secret-key-in-production

# Optional — only needed for advanced setups (reverse proxy, custom ports):
# ALLOWED_ORIGINS=https://example.com
# NEXT_PUBLIC_API_URL=https://api.example.com
